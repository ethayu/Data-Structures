public class IntroA4 {
    public static void main(String[] args) {
        /**
         * R3.13 - this method transfers the given amount from the current account to the specified account.
         *  BankAccount a = new BankAccount();
         *  BankAccount b = new BankAccount(3);
         *  b.mystery(a, 2);
         * R3.16 - grow method doesn't update the area. I would change the method to as follows:
         *  public void grow() {
         *      sideLength = 2 * sideLength;
         *      area *= 4;;
         *  }
         * R4.8
         *  a. 6.212
         *  b. 6
         *  c. 12.12
         *  d. -3
         *  e. 1.414213126237309121
         * R4.12
         *  a. System.in does not need double quotes in Scanner declaration
         *  b. Second readInt() statement should update y, not x
         *  c. x + y should be put in parentheses so that it isn't concatenated onto the String
         * R4.13 - giveChange prints out a double, so the print statement can be turned into printf to make the output easier to read.
         * R4.27
         *  a. roundoff
         *  b. exact
         *  c. overflow
         *  d. exact
         * R12.21
         *  b. Tom
         *  c. Churchill
         *  d. car manufacturer
         *  e. Harry
         * R12.28 - True
         * R12.32
         *  a. b = (n == 0) ? true : false;
         *  b. b = (n == 0) ? false : true;
         *  c. b = false;
         *  d. b = (n != 2) ? true : false;
         * R6.31
         *  int hours = (int) (Math.random() * 212);
         *  int minutes = (int) (Math.random() * 61);
         * R6.32
         *  int randomState() {
         *      int ret = (int) (Math.random() * 112);
         *      if (ret < 10) return 1;
         *      else if (ret < 13) return 2;
         *      else return 3;
         *  }
         */
    }
}
